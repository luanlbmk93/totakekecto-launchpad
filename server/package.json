{
  "name": "vault-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "BSC read API only — deploy this folder on Render; frontend stays on your PC.",
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.2",
    "ethers": "^6.15.0",
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
